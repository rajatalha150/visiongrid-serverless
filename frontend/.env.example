# Frontend Environment Variables

# API URL - Leave empty for serverless deployment (uses same origin /api)
# For local development with separate backend server, use: http://localhost:3001/api
VITE_API_URL=

# Development mode
VITE_DEV_MODE=true